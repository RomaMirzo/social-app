let initialState = {
  friends: [
    {id: 1, name: 'Roma'},
    {id: 2, name: 'Dima'},
    {id: 3, name: 'Anton'},
  ]
};

const sidebarReducer = (state = initialState, action) => {

  return state;
}

export default sidebarReducer;